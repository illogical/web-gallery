// TODO: GUI for the selected image or video

import React from "react"

interface FocusBarProps {

}

export const FocusBar: React.FC<FocusBarProps> = ({}) => {
    return (
        <React.Fragment>

        </React.Fragment>
    )
}